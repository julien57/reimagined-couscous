{% extends 'base.html.twig' %}

{% block title %}Password Reset Email Sent{% endblock %}

{% block body %}
<div id="login-form">
    <div class="form col-md-6">
        <div class="header-logo flex-col mb-3 m-auto">
            <div class="_logo"></div>
        </div>
    <p class="mt-3">
        S'il existe un compte correspondant à votre e-mail,
        un e-mail vient d'être envoyé contenant un lien que vous pouvez utiliser pour réinitialiser votre mot de passe.
        Ce lien expirera dans
        {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
    </p>
    <p>Si vous ne recevez pas d'e-mail, veuillez vérifier votre dossier spam ou <br><a href="{{ path('app_forgot_password_request') }}">try again</a>.</p>

    <form>
        <a href="{{ path('app_login') }}" class="btn btn-primary float-right mt-4">Aller au formulaire de connexion</a>
    </form>
</div>
{% endblock %}
